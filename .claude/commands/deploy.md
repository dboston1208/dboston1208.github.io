---
description: Merge draft into main, push main, and switch back to draft
---

Do the following, in order, using Bash:

1. Switch to the `main` branch.
2. Merge the `draft` branch into `main`.
3. Push `main` to GitHub (`origin`).
4. Switch back to the `draft` branch.

If any step fails (e.g. a merge conflict), stop and report the problem instead of continuing.

Once all steps succeed, tell the user to check https://dboston1208.github.io in a minute or two to confirm the changes are live.
